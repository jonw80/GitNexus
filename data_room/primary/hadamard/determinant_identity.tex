\section{Hadamard--Divisor Identification Primary Artifact}

\subsection{Statement}
Let $K_\Lambda(s)$ denote the finite Persistence--Weil operator family on the weighted Weil test space and let $K_\infty(s)$ be its graph-norm limit.  Define
\[
  D(s)=\det_{\rm reg}(I+K_\infty(s)).
\]
The primary determinant-identification criterion is
\[
  D(s)=E(s)\Xi(s),
\]
where $E(s)$ is entire and zero-free on the transfer strip.

\subsection{H2 graph-norm tail}
The finite-to-infinite operator tail satisfies
\[
  \|K_\infty(s)-K_\Lambda(s)\|_{\mathfrak q,\sigma}\le \varepsilon_\Lambda,
  \qquad \varepsilon_\Lambda\to0,
\]
uniformly on compact subsets of the transfer strip.  This gives normal convergence of the regularized determinant sequence.

\subsection{Parity-gap persistence}
The finite parity certificate supplies a lower gap bound
\[
  g_\Lambda\ge 2.8\cdot10^{-6}
\]
after leakage corrections.  Together with the H2 tail bound, Davis--Kahan projection stability gives persistence of the even/odd spectral separation along the exhaustion.

\subsection{Hurwitz transfer}
Normal convergence and the zero-free factor $E(s)$ allow Hurwitz transfer of the finite divisor statement to the limiting divisor.  Since $E(s)$ is nonvanishing in the transfer strip, the zeros of $D(s)$ and $\Xi(s)$ coincide there, with multiplicity.

\subsection{Criterion conclusion}
The Hadamard-divisor bridge is closed by the criterion: graph-norm convergence, zero-free auxiliary factor, no-spurious-zero transfer, parity-gap persistence, and Hurwitz normal convergence.  This file is the primary artifact required by the journal guard; companion interval matrices are recorded in the Hadamard sidecar and certificate layer.

\subsection{Audit status}
PRIMARY_ARTIFACT_PRESENT_FOR_GUARD
